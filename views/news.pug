extends layout

block content
  h1= title

  form(method='get')
    input(type="text" name="search")
    input(type="submit" name="search" value='Search')

  each item in data 
    article
      h1=item.title
      p=item.created
      p=item.description
